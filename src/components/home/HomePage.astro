---
import Layout from "../../layouts/Layout.astro";
import ModelCloud from "../islands/ModelCloud.svelte";
import HomeNav from "./HomeNav.astro";
import About from "./sections/About.astro";
import Contact from "./sections/Contact.astro";
import Focus from "./sections/Focus.astro";
import HomeLab from "./sections/HomeLab.astro";
import Intro from "./sections/Intro.astro";
import Model from "./sections/Model.astro";
import Path from "./sections/Path.astro";
import Work from "./sections/Work.astro";
---

<Layout>
  <div class="scroll-progress" aria-hidden="true"><i></i></div>

  <div class="model-bg" aria-hidden="true">
    <ModelCloud client:idle src="/me.glb" lockLook={true} shiftX={1.25} disableHiRes={true}/>
  </div>

  <HomeNav />

  <main class="content">
    <Intro />
    <About />
    <Focus />
    <Work />
    <HomeLab />
    <Path />
    <Model />
    <Contact />
  </main>

</Layout>

<style is:global>
  /* Pinned 3D model: supporting right column on desktop, full-bleed backdrop on mobile */
  /* full-viewport canvas so the scatter intro is never clipped; the model
     itself is pushed to the right via the component's shiftX prop */
  .model-bg {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }
  /* soft halo so the figure reads as lit, not floating in void */
  .model-bg::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
      closest-side at 74% 50%,
      rgba(232, 179, 74, 0.1),
      rgba(122, 162, 247, 0.05) 30%,
      transparent 52%
    );
    pointer-events: none;
  }
  /* manual look is locked, so no drag affordance is needed */
  .model-bg canvas {
    pointer-events: none;
    cursor: default;
  }

  /* Thin scroll-progress bar (CSS scroll-driven; degrades to hidden) */
  .scroll-progress {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    z-index: 60;
    pointer-events: none;
  }
  .scroll-progress i {
    display: block;
    height: 100%;
    width: 100%;
    transform-origin: 0 50%;
    transform: scaleX(0);
    background: linear-gradient(90deg, var(--amber), var(--mint));
    animation: progress linear;
    animation-timeline: scroll(root);
  }
  @keyframes progress {
    to {
      transform: scaleX(1);
    }
  }

  /* ── Scroll-reveal: panel content slides up, fades, and de-blurs in a
     cascading stagger as each section enters the viewport. The .reveal class
     and per-item --i stagger index are assigned by script; we only animate
     when motion is welcome. ───────────────────────────────────────────────── */
  @media (prefers-reduced-motion: no-preference) {
    .panel .reveal {
      opacity: 0;
      transform: translateY(34px);
      filter: blur(8px);
      transition:
        opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1),
        transform 0.85s cubic-bezier(0.16, 1, 0.3, 1),
        filter 0.85s cubic-bezier(0.16, 1, 0.3, 1);
      transition-delay: calc(var(--i, 0) * 75ms);
      will-change: opacity, transform, filter;
    }
    .panel.in .reveal {
      opacity: 1;
      transform: none;
      filter: blur(0);
    }
    /* once revealed, drop the hint so hover/idle transforms aren't fought */
    .panel.in .reveal.done {
      will-change: auto;
    }
  }

  /* Top navigation */
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 13px 28px;
    border-bottom: 1px solid color-mix(in srgb, var(--line) 72%, transparent);
    background: color-mix(in srgb, var(--bg-0) 78%, transparent);
    backdrop-filter: blur(16px) saturate(130%);
    -webkit-backdrop-filter: blur(16px) saturate(130%);
    font-size: 12px;
  }
  .brand {
    color: var(--amber);
    text-decoration: none;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 8px;
    letter-spacing: 0.02em;
  }
  .brand .ext {
    color: var(--fg-mute);
    font-weight: 400;
  }
  .brand .dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--mint);
    box-shadow: 0 0 8px var(--mint);
    animation: pulse 2s infinite;
  }
  .nav .links {
    display: flex;
    gap: 18px;
  }
  .nav .links a {
    position: relative;
    padding: 5px 0;
    color: var(--fg-mute);
    text-decoration: none;
    transition: color 160ms;
  }
  .nav .links a::after {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    height: 1px;
    background: var(--amber);
    content: "";
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 180ms ease;
  }
  .nav .links a:hover {
    color: var(--fg);
  }
  .nav .links a:hover::after,
  .nav .links a.active::after {
    transform: scaleX(1);
    transform-origin: left;
  }
  .nav .links a.active {
    color: var(--amber);
  }
  .brand-group {
    display: flex;
    align-items: center;
    gap: 14px;
  }
  .nav a.resume {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    color: var(--amber);
    text-decoration: none;
    border: 1px solid var(--line-strong);
    border-radius: 999px;
    padding: 4px 11px;
    transition:
      color 140ms,
      border-color 140ms;
  }
  .nav a.resume:hover {
    color: var(--fg);
    border-color: var(--amber);
  }

  /* Hamburger toggle: hidden on desktop, shown on mobile */
  .nav-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 4px;
    width: 34px;
    height: 34px;
    padding: 0;
    background: transparent;
    border: 1px solid var(--line);
    border-radius: 4px;
    cursor: pointer;
  }
  .nav-toggle span {
    display: block;
    width: 16px;
    height: 1.5px;
    margin: 0 auto;
    background: var(--fg-soft);
    transition:
      transform 180ms ease,
      opacity 140ms ease;
  }
  .nav.open .nav-toggle span:nth-child(1) {
    transform: translateY(5.5px) rotate(45deg);
  }
  .nav.open .nav-toggle span:nth-child(2) {
    opacity: 0;
  }
  .nav.open .nav-toggle span:nth-child(3) {
    transform: translateY(-5.5px) rotate(-45deg);
  }

  /* Scrolling content column: primary two-thirds on desktop */
  .content {
    position: relative;
    z-index: 2;
    width: min(64%, 1040px);
    padding: 0 clamp(28px, 4vw, 64px);
  }

  .panel {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 96px 0;
    gap: 14px;
    scroll-snap-align: start;
    scroll-snap-stop: normal;
  }
  .panel:not(.hero)::before {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    height: 1px;
    background: linear-gradient(
      90deg,
      var(--amber),
      color-mix(in srgb, var(--line) 75%, transparent) 18%,
      transparent 82%
    );
    content: "";
    opacity: 0.52;
  }

  .panel h2 {
    font-family: var(--serif);
    font-size: clamp(30px, 4.5vw, 52px);
    line-height: 1.02;
    margin: 4px 0 8px;
    font-weight: 400;
    color: var(--fg);
    text-wrap: balance;
  }
  .panel p {
    color: var(--fg-soft);
    font-size: 14.5px;
    line-height: 1.7;
    max-width: 52ch;
    margin: 0;
  }
  .panel p a {
    color: var(--amber);
    text-decoration: none;
    border-bottom: 1px solid var(--line-strong);
    transition: border-color 140ms;
  }
  .panel p a:hover {
    border-color: var(--amber);
  }

  /* Hero */
  .hero .availability {
    font-size: 10.5px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--fg-mute);
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 6px 10px;
    max-width: none;
  }
  .hero .availability .stat {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: var(--mint);
  }
  .hero .availability .stat::before {
    content: "";
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--mint);
    box-shadow: 0 0 7px var(--mint);
  }
  .name {
    font-family: var(--serif);
    font-size: clamp(52px, 8vw, 104px);
    line-height: 0.92;
    letter-spacing: -0.02em;
    margin: 6px 0 18px;
    color: var(--fg);
    font-weight: 400;
    text-wrap: balance;
    text-shadow: 0 12px 48px rgb(0 0 0 / 36%);
  }
  .name .em {
    font-style: italic;
    color: var(--amber);
  }
  .lede {
    font-size: 16.5px !important;
    max-width: 46ch !important;
  }
  .lede .k {
    color: var(--amber);
  }
  .lede .s {
    color: var(--mint);
  }

  .cta {
    display: flex;
    gap: 12px;
    margin-top: 26px;
  }
  .btn {
    padding: 10px 20px;
    border-radius: 3px;
    border: 1px solid var(--line-strong);
    color: var(--fg-soft);
    text-decoration: none;
    font-size: 12px;
    letter-spacing: 0.04em;
    transition:
      color 160ms,
      border-color 160ms,
      background 160ms,
      box-shadow 160ms,
      transform 160ms;
  }
  .btn:hover {
    border-color: var(--amber);
    color: var(--fg);
    transform: translateY(-2px);
  }
  .btn.primary {
    background: var(--amber);
    color: var(--bg-0);
    border-color: var(--amber);
    font-weight: 600;
  }
  .btn.primary:hover {
    filter: brightness(1.08);
    box-shadow: 0 8px 28px rgb(232 179 74 / 18%);
  }

  .scroll-hint {
    margin-top: 48px;
    font-size: 10.5px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--fg-mute);
  }
  .scroll-hint span {
    display: inline-block;
    animation: bob 1.6s ease-in-out infinite;
  }
  @keyframes bob {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(4px);
    }
  }

  /* Focus cards */
  .cards {
    display: grid;
    gap: 12px;
    margin-top: 12px;
  }
  .card {
    position: relative;
    overflow: hidden;
    border: 1px solid var(--line);
    background: color-mix(in srgb, var(--bg-1) 78%, transparent);
    backdrop-filter: blur(6px);
    border-radius: 8px;
    padding: 19px 20px;
    transition:
      border-color 180ms,
      background 180ms,
      transform 180ms;
  }
  .card:hover {
    border-color: color-mix(in srgb, var(--amber) 42%, var(--line));
    background: color-mix(in srgb, var(--bg-1) 90%, transparent);
    transform: translateX(4px);
  }
  .card h3 {
    margin: 0 0 6px;
    font-size: 13px;
    color: var(--mint);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .card p {
    font-size: 13px;
  }
  .tags,
  .meta {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 6px;
    padding: 0;
    margin: 12px 0 0;
  }
  .tags li,
  .meta li {
    font-size: 10.5px;
    padding: 2px 8px;
    border-radius: 2px;
    background: var(--bg-3);
    color: var(--fg-soft);
    letter-spacing: 0.04em;
  }
  .meta li {
    display: inline-flex;
    align-items: center;
    height: 24px;
    padding: 0 8px;
  }
  .meta li.meta-logo {
    padding: 0 6px;
  }
  .meta-logo a {
    display: inline-flex;
    align-items: center;
  }
  .meta-icon {
    display: block;
    height: 20px;
    width: auto;
  }

  /* Work list */
  .work-list {
    display: flex;
    flex-direction: column;
    gap: 4px;
    margin-top: 10px;
  }
  .work-item {
    margin: 0 -14px;
    padding: 22px 14px;
    border-top: 1px solid var(--line);
    border-radius: 7px;
    transition:
      background 180ms,
      border-color 180ms,
      transform 180ms;
  }
  .work-item:hover {
    border-color: color-mix(in srgb, var(--amber) 35%, var(--line));
    background: color-mix(in srgb, var(--bg-1) 74%, transparent);
    transform: translateX(4px);
  }
  .work-head {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 0 0 6px;
  }
  .work-head .seer-attn {
    margin: 0;
    padding: 0;
  }
  .work-item h3 {
    margin: 0;
    font-size: 17px;
    color: var(--fg);
    font-weight: 600;
  }
  .work-item h3 a {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: var(--amber);
    text-decoration: none;
    transition: color 140ms;
  }
  .work-item h3 a:hover {
    color: var(--fg);
  }
  .work-item p {
    font-size: 13px;
  }
  .work-proof {
    display: inline-block;
    margin-top: 12px;
    color: var(--fg-mute);
    font-size: 10.5px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  /* Timeline */
  .timeline {
    list-style: none;
    margin: 12px 0 0;
    padding-left: 26px;
    border-left: 1px solid var(--line);
  }
  .timeline li {
    position: relative;
    padding: 0 0 26px;
  }
  .timeline li::before {
    content: "";
    position: absolute;
    left: -32px;
    top: 6px;
    width: 11px;
    height: 11px;
    border-radius: 50%;
    background: var(--bg-0);
    border: 2px solid var(--amber);
  }
  .timeline li.cur::before {
    background: var(--amber);
    box-shadow: 0 0 12px var(--amber);
    animation: pulse 1.6s infinite;
  }
  .timeline .when {
    font-size: 10.5px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--fg-mute);
  }
  .timeline h3 {
    margin: 4px 0 4px;
    font-size: 15px;
    color: var(--fg);
    font-weight: 600;
  }
  .timeline h3 .at {
    color: var(--amber);
    font-weight: 400;
  }
  .timeline p {
    font-size: 12.5px;
  }

  /* GPU render (model section) */
  .gpu-shot {
    margin: 26px 0 0;
    border: 1px solid var(--line);
    border-radius: 6px;
    overflow: hidden;
    background: #000;
    box-shadow: 0 24px 64px rgb(0 0 0 / 20%);
  }
  .gpu-shot img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    transition:
      filter 300ms,
      transform 500ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .gpu-shot:hover img {
    filter: saturate(1.08) contrast(1.03);
    transform: scale(1.015);
  }
  .gpu-composite {
    position: relative;
  }
  .gpu-composite .glass-inset {
    position: absolute;
    top: 14px;
    right: 14px;
    width: clamp(116px, 27%, 168px);
    height: auto;
    aspect-ratio: 1;
    border: 1px solid color-mix(in srgb, var(--violet) 58%, var(--line));
    border-radius: 5px;
    object-fit: cover;
    box-shadow:
      0 12px 32px rgb(0 0 0 / 55%),
      0 0 28px rgb(196 166 247 / 18%);
  }
  @media (max-width: 560px) {
    .gpu-shot img {
      aspect-ratio: 4 / 3;
    }
    .gpu-composite .glass-inset {
      top: 10px;
      right: 10px;
      width: 34%;
      aspect-ratio: 1;
    }
  }
  .gpu-shot figcaption {
    padding: 9px 14px;
    font-size: 11px;
    color: var(--fg-mute);
    letter-spacing: 0.02em;
    border-top: 1px solid var(--line);
  }

  /* Built-with logos (model section) */
  .builtwith {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 16px 24px;
    margin-top: 26px;
  }
  .builtwith-label {
    font-size: 10.5px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--fg-mute);
  }
  .logos {
    display: flex;
    align-items: center;
    gap: 22px;
  }
  .logo {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    text-decoration: none;
    opacity: 0.85;
    transition: opacity 160ms;
  }
  a.logo:hover {
    opacity: 1;
  }
  .logo img {
    display: block;
    height: 20px;
    width: auto;
  }
  .logo.gpu span {
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--fg);
  }

  /* Contact */
  .contact-paths {
    display: grid;
    gap: 8px;
    margin-top: 20px;
  }
  .contact-path {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    gap: 18px;
    padding: 16px 0;
    border-bottom: 1px solid var(--line);
    color: var(--fg-soft);
    text-decoration: none;
    transition:
      color 160ms,
      border-color 160ms,
      padding 180ms;
  }
  .contact-path .icon-arrow {
    grid-column: 2;
    grid-row: 1 / 3;
    color: var(--amber);
  }
  .contact-path:hover {
    color: var(--amber);
    border-color: var(--amber);
    padding-inline: 8px;
  }
  .contact-path span {
    font-size: 14px;
  }
  .contact-path small {
    grid-column: 1;
    color: var(--fg-mute);
    font-size: 11px;
  }
  .contact-path.primary span {
    color: var(--amber);
  }
  .social-links {
    display: flex;
    gap: 18px;
    margin-top: 24px;
  }
  .social-links a {
    color: var(--fg-mute);
    font-size: 11px;
    text-underline-offset: 4px;
    transition: color 160ms;
  }
  .social-links a:hover {
    color: var(--fg);
  }
  .contact footer {
    margin-top: 56px;
    font-size: 11px;
    color: var(--fg-faint);
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.4;
    }
  }

  /* Joke button + reveal modal */
  .btn.joke {
    cursor: pointer;
    font-family: inherit;
    appearance: none;
    background: transparent;
    color: var(--fg-soft);
  }
  .joke-emoji {
    display: block;
    width: 56px;
    height: 56px;
    margin: 0 auto 12px;
    transform: rotate(180deg);
  }
  .joke-reveal h3 {
    font-family: var(--serif);
    font-size: 22px;
    font-weight: 400;
    color: var(--fg);
    margin: 0 0 10px;
  }
  .joke-reveal h3 em {
    color: var(--amber);
    font-style: italic;
  }
  .joke-reveal p {
    font-size: 13.5px;
    line-height: 1.7;
    color: var(--fg-soft);
    margin: 0 auto 20px;
    max-width: 36ch;
  }
  .joke-reveal .btn {
    cursor: pointer;
    font-family: inherit;
    display: inline-block;
  }

  /* Tablet / mobile: model becomes a faint backdrop, content goes full width */
  @media (max-width: 900px) {
    .model-bg {
      width: 100vw;
      opacity: 0.2;
    }
    .model-bg canvas {
      pointer-events: none;
    }
    .content {
      width: 100%;
      padding: 0 clamp(22px, 6vw, 48px);
    }
    .content::before {
      content: "";
      position: fixed;
      inset: 0;
      z-index: -1;
      background: linear-gradient(90deg, var(--bg-0) 30%, transparent);
      pointer-events: none;
    }
    .panel {
      min-height: auto;
      padding: 80px 0;
    }
    #top {
      min-height: 100vh;
    }
    .nav .links {
      gap: 12px;
      font-size: 11px;
    }
  }
  @media (max-width: 560px) {
    .nav {
      padding: 12px 16px;
    }
    .nav-toggle {
      display: flex;
      width: 44px;
      height: 44px;
    }
    /* Collapse the inline links into a dropdown panel */
    .nav .links {
      position: absolute;
      top: calc(100% - 4px);
      right: 16px;
      flex-direction: column;
      align-items: flex-end;
      gap: 14px;
      padding: 16px 18px;
      border: 1px solid var(--line-strong);
      border-radius: 6px;
      background: color-mix(in srgb, var(--bg-1) 92%, transparent);
      backdrop-filter: blur(10px);
      font-size: 13px;
      opacity: 0;
      transform: translateY(-6px);
      pointer-events: none;
      transition:
        opacity 160ms ease,
        transform 160ms ease;
    }
    .nav.open .links {
      opacity: 1;
      transform: translateY(0);
      pointer-events: auto;
    }
    .btn.joke {
      display: none;
    }
  }
</style>

<script>
  // Scrollspy: highlight the nav link for the section currently in view.
  const links = [
    ...document.querySelectorAll<HTMLAnchorElement>(".nav .links a"),
  ];
  const byId = new Map(links.map((a) => [a.getAttribute("href")!.slice(1), a]));
  const navSection = new Map([
    ["about", "top"],
    ["focus", "top"],
    ["homelab", "work"],
    ["experiments", "work"],
  ]);
  const obs = new IntersectionObserver(
    (entries) => {
      for (const e of entries) {
        if (e.isIntersecting) {
          links.forEach((l) => l.classList.remove("active"));
          const sectionId = (e.target as HTMLElement).id;
          byId.get(navSection.get(sectionId) ?? sectionId)?.classList.add("active");
        }
      }
    },
    { rootMargin: "-45% 0px -45% 0px" },
  );
  document
    .querySelectorAll("main.content section[id]")
    .forEach((s) => obs.observe(s));

  // Scroll-reveal: tag each panel's content with .reveal + a stagger index,
  // then flip the panel to .in the first time it scrolls into view. Grids are
  // expanded into their items so cards/work/timeline rows cascade individually.
  const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const panels = [
    ...document.querySelectorAll<HTMLElement>("main.content .panel"),
  ];
  if (!reduce) {
    for (const panel of panels) {
      const targets: HTMLElement[] = [];
      for (const child of [...panel.children] as HTMLElement[]) {
        if (child.matches(".cards, .work-list, .timeline, .lab-gallery")) {
          targets.push(...([...child.children] as HTMLElement[]));
        } else {
          targets.push(child);
        }
      }
      targets.forEach((el, i) => {
        el.classList.add("reveal");
        el.style.setProperty("--i", String(i));
        el.addEventListener(
          "transitionend",
          () => el.classList.add("done"),
          { once: true },
        );
      });
    }
    const revealObs = new IntersectionObserver(
      (entries) => {
        for (const e of entries) {
          if (e.isIntersecting) {
            e.target.classList.add("in");
            revealObs.unobserve(e.target);
          }
        }
      },
      { rootMargin: "0px 0px -12% 0px", threshold: 0.08 },
    );
    panels.forEach((p) => revealObs.observe(p));
  }

  // Mobile nav: hamburger toggles the collapsed link panel.
  const navEl = document.querySelector<HTMLElement>(".nav");
  const navToggle = document.getElementById("navToggle");
  const setNav = (open: boolean) => {
    navEl?.classList.toggle("open", open);
    navToggle?.setAttribute("aria-expanded", String(open));
    navToggle?.setAttribute(
      "aria-label",
      open ? "Close navigation" : "Open navigation",
    );
  };
  navToggle?.addEventListener("click", (e) => {
    e.stopPropagation();
    setNav(!navEl?.classList.contains("open"));
  });
  links.forEach((a) => a.addEventListener("click", () => setNav(false)));
  document.addEventListener("click", (e) => {
    if (
      navEl?.classList.contains("open") &&
      !navEl.contains(e.target as Node)
    ) {
      setNav(false);
    }
  });

  // Escape closes the mobile nav (the Modal island handles its own dismissal).
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") setNav(false);
  });
</script>
